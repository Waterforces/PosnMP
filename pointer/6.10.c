#include<stdio.h> //NOt finish
#include<string.h>
int main() {
	char str[1000],let[1000];
	int x,y,i,j,c=0;
	gets(str);
	gets(let);
	x=strlen(str);
	y=strlen(let);
	for(i=0;i<x;i++) {
		
	}
}